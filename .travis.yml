sudo: false
language: cpp

matrix:
  include:
    - os: linux
      compiler: clang
      addons: &clang38
        apt:
          sources: ['llvm-toolchain-precise-3.8', 'ubuntu-toolchain-r-test']
          packages: ['clang-3.8', 'libclang-3.8-dev']
      env: COMPILER='clang++-3.8'
    
    - os: osx
      osx_image: xcode7
      compiler: clang
      env: COMPILER='clang++'

install:
  - pip install --user clang django nose
  - gem install ffi
  - DEPS_DIR="${TRAVIS_BUILD_DIR}/deps"
  - mkdir ${DEPS_DIR} && cd ${DEPS_DIR}
  - |
    if [[ "${TRAVIS_OS_NAME}" == "linux" ]]; then
      CMAKE_URL="http://www.cmake.org/files/v3.3/cmake-3.3.2-Linux-x86_64.tar.gz"
      mkdir cmake && travis_retry wget --no-check-certificate --quiet -O - ${CMAKE_URL} | tar --strip-components=1 -xz -C cmake
      export PATH=${DEPS_DIR}/cmake/bin:${PATH}
    elif [[ "${TRAVIS_OS_NAME}" == "osx" ]]; then
      brew install cmake
    fi
  - cd ${TRAVIS_BUILD_DIR}

before_script:
  - export CXX=${COMPILER}
  - |
    if [[ "${TRAVIS_OS_NAME}" == "linux" ]]; then
      export LD_LIBRARY_PATH=$(dirname $(find /usr/lib/llvm-3.8 -name libclang.so))
    elif [[ "${TRAVIS_OS_NAME}" == "osx" ]]; then
      export LD_LIBRARY_PATH=$(dirname $(find /Applications/Xcode.app/Contents/ -name libclang.dylib)):$LD_LIBRARY_PATH
    fi
script: ${TRAVIS_BUILD_DIR}/run_tests.sh

notifications:
  slack:
    rooms:
      - c-api:ZTxxqvEcr25iaaB8XBS7luaA#general

